# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build backends--ops-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t backends--ops-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system backends--ops-backend && \
          adduser --system -G backends--ops-backend backends--ops-backend

COPY dist/packages/backends/ops-backend backends--ops-backend
RUN chown -R backends--ops-backend:backends--ops-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix backends--ops-backend --omit=dev -f install

CMD [ "node", "backends--ops-backend" ]
